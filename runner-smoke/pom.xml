<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.apache.tomee.cts</groupId>
        <artifactId>tomee-jakartaee11-tck</artifactId>
        <version>11.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>jakarta-ee-11-webprofile-smoke-runner</artifactId>
    <packaging>jar</packaging>

    <name>TomEE Jakarta EE 11 Web Profile TCK Smoke Runner</name>

    <properties>
        <!-- A small Web Profile test which creates and deploys one JAX-RS WAR. -->
        <smoke.test>com.sun.ts.tests.jaxrs.platform.servletconfig.JAXRSClientIT</smoke.test>
        <!-- Container bind ports; run-smoke-suite.sh overrides these with
             free ports selected at branch start. -->
        <tomee.http.port>8080</tomee.http.port>
        <tomee.shutdown.port>8005</tomee.shutdown.port>
    </properties>

    <dependencies>
        <!-- Published Jakarta EE 11 Platform TCK test artifact. -->
        <dependency>
            <groupId>jakarta.tck</groupId>
            <artifactId>rest-platform-tck</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.apache.tomee.cts</groupId>
            <artifactId>tomee-porting</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.jboss.arquillian.junit5</groupId>
            <artifactId>arquillian-junit5-container</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.tomee</groupId>
            <artifactId>arquillian-tomee-remote</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <testResources>
            <!-- Filter arquillian.xml so the TomEE version and distribution
                 classifier follow the reactor properties. -->
            <testResource>
                <directory>src/test/resources</directory>
                <filtering>true</filtering>
            </testResource>
        </testResources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <!-- Assert the effective container ports are free
                             inside the build, immediately before the remote
                             adapter starts TomEE. This asserts ${tomee.*.port}
                             as Maven resolves them, so a caller -D override
                             (last-wins on the mvnw command line) is guarded on
                             its actual value, and the check runs after the
                             reactor build rather than in the wrapper script, so
                             a daemon binding the port during the build cannot
                             slip past. Without it the adapter would silently
                             attach to a foreign server already on the port. -->
                        <id>require-free-tomee-ports</id>
                        <phase>validate</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <executable>sh</executable>
                            <arguments>
                                <argument>${maven.multiModuleProjectDirectory}/environment/tomee/require-tomee-ports-free.sh</argument>
                                <argument>${tomee.http.port}</argument>
                                <argument></argument>
                                <argument>${tomee.shutdown.port}</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>resolve-tomee-distribution</id>
                        <phase>generate-test-resources</phase>
                        <goals><goal>get</goal></goals>
                        <configuration>
                            <artifact>org.apache.tomee:apache-tomee:${tomee.version}:zip:${tomee.classifier}</artifact>
                            <transitive>false</transitive>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <dependenciesToScan>
                        <dependency>jakarta.tck:rest-platform-tck</dependency>
                    </dependenciesToScan>
                    <includes>
                        <include>**/${smoke.test}.java</include>
                    </includes>
                    <systemPropertyVariables>
                        <webServerHost>localhost</webServerHost>
                        <webServerPort>${tomee.http.port}</webServerPort>
                        <junit.log.traceflag>true</junit.log.traceflag>
                    </systemPropertyVariables>
                </configuration>
                <executions>
                    <execution>
                        <id>tck-smoke</id>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
