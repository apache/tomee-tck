<?xml version="1.0" encoding="UTF-8"?>
<!--
    Licensed to the Apache Software Foundation (ASF) under one or more
    contributor license agreements. See the NOTICE file distributed with
    this work for additional information regarding copyright ownership.
    The ASF licenses this file to You under the Apache License, Version 2.0.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.apache.tomee.cts</groupId>
    <artifactId>runner-standalone</artifactId>
    <version>11.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>runner-standalone-jsonb</artifactId>
  <name>Jakarta JSON Binding 3.0 TCK runner</name>
  <description>
    Runs the standalone Jakarta JSON Binding 3.0 TCK (functional tests and
    the signature test) against Apache Johnzon, the JSON-B provider shipped
    by TomEE. The TCK runs in the local JVM; no server is started. The
    signature test checks the jakarta.json.bind packages of the
    jakartaee-api jar bundled in the selected TomEE distribution.
  </description>

  <properties>
    <!-- Provider-level TCK: no TomEE container or Derby lifecycle. -->
    <tck.standalone.container.skip>true</tck.standalone.container.skip>
    <jsonb.tck.version>3.0.0</jsonb.tck.version>
    <!-- Reviewed known-gap exclusions; point at exclusions/none.txt for a
         full baseline run. -->
    <tck.exclusions.file>${project.basedir}/../exclusions/jsonb.txt</tck.exclusions.file>
  </properties>

  <dependencies>
    <!-- The provider under test, exactly the version bundled in the TomEE
         distribution (the bundled jars are the Central release bytes with
         re-stamped zip entries). Pulls johnzon-mapper, johnzon-core, and
         the Jakarta JSON APIs transitively. -->
    <dependency>
      <groupId>org.apache.johnzon</groupId>
      <artifactId>johnzon-jsonb</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Johnzon declares the APIs as provided; these are the versions
         Johnzon 2.1.0 is built against. -->
    <dependency>
      <groupId>jakarta.json.bind</groupId>
      <artifactId>jakarta.json.bind-api</artifactId>
      <version>3.0.1</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>jakarta.json</groupId>
      <artifactId>jakarta.json-api</artifactId>
      <version>2.1.3</version>
      <scope>test</scope>
    </dependency>
    <!-- The TCK's CDI tests bootstrap a CDI SE container; use Apache
         OpenWebBeans, the CDI implementation TomEE ships (mirrors the di
         runner). -->
    <dependency>
      <groupId>org.apache.openwebbeans</groupId>
      <artifactId>openwebbeans-se</artifactId>
      <version>${openwebbeans.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>jakarta.enterprise</groupId>
      <artifactId>jakarta.enterprise.cdi-api</artifactId>
      <version>4.1.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>jakarta.interceptor</groupId>
      <artifactId>jakarta.interceptor-api</artifactId>
      <version>2.2.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>jakarta.annotation</groupId>
      <artifactId>jakarta.annotation-api</artifactId>
      <version>3.0.0</version>
      <scope>test</scope>
    </dependency>
    <!-- com.sun.tdk classes used by the TCK's bundled signature test
         driver; replaces the JDK-21-incapable org.netbeans.tools sigtest
         declared by the TCK pom. -->
    <dependency>
      <groupId>jakarta.tck</groupId>
      <artifactId>sigtest-maven-plugin</artifactId>
      <version>2.6</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>stage-tomee-api-jar</id>
            <phase>generate-test-resources</phase>
            <goals><goal>unpack</goal></goals>
            <configuration>
              <skip>${tck.standalone.skip}</skip>
              <artifactItems>
                <artifactItem>
                  <groupId>org.apache.tomee</groupId>
                  <artifactId>apache-tomee</artifactId>
                  <version>${tomee.version}</version>
                  <classifier>${tomee.classifier}</classifier>
                  <type>zip</type>
                  <outputDirectory>${project.build.directory}/tomee</outputDirectory>
                  <includes>**/lib/jakartaee-api-*.jar</includes>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>3.1.0</version>
        <executions>
          <execution>
            <id>locate-tomee-api-jar</id>
            <phase>generate-test-resources</phase>
            <goals><goal>run</goal></goals>
            <configuration>
              <skip>${tck.standalone.skip}</skip>
              <exportAntProperties>true</exportAntProperties>
              <target>
                <path id="tomee-api-jar">
                  <fileset dir="${project.build.directory}/tomee">
                    <include name="**/lib/jakartaee-api-*.jar"/>
                  </fileset>
                </path>
                <pathconvert pathsep="${path.separator}"
                             property="jsonb.sig.api.jar"
                             refid="tomee-api-jar"/>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <skip>${tck.standalone.skip}</skip>
          <excludesFile>${tck.exclusions.file}</excludesFile>
          <dependenciesToScan>
            <dependency>jakarta.json.bind:jakarta.json.bind-tck</dependency>
          </dependenciesToScan>
          <systemPropertyVariables>
            <!-- Registers the CdiContainerExtension that boots OpenWebBeans
                 for the TCK's CDI tests. -->
            <junit.jupiter.extensions.autodetection.enabled>true</junit.jupiter.extensions.autodetection.enabled>
            <jimage.dir>${project.build.directory}/jimage</jimage.dir>
            <signature.sigTestClasspath>${jsonb.sig.api.jar}${path.separator}${project.build.directory}/jimage/java.base${path.separator}${project.build.directory}/jimage/java.rmi${path.separator}${project.build.directory}/jimage/java.sql${path.separator}${project.build.directory}/jimage/java.naming</signature.sigTestClasspath>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <!-- The TCK artifact resolves from Maven Central (byte-identical to
           the EFTL distribution zip; see environment/versions.env for the
           zip checksum), but stays out of the default reactor so a default
           build downloads no TCK material. -->
      <id>standalone-tck</id>
      <activation>
        <property>
          <name>tck.standalone</name>
          <value>true</value>
        </property>
      </activation>
      <dependencies>
        <dependency>
          <groupId>jakarta.json.bind</groupId>
          <artifactId>jakarta.json.bind-tck</artifactId>
          <version>${jsonb.tck.version}</version>
          <scope>test</scope>
          <exclusions>
            <!-- Cannot parse JDK 21 class files; the jakarta.tck fork
                 above provides the same com.sun.tdk packages. -->
            <exclusion>
              <groupId>org.netbeans.tools</groupId>
              <artifactId>sigtest-maven-plugin</artifactId>
            </exclusion>
          </exclusions>
        </dependency>
      </dependencies>
    </profile>
  </profiles>
</project>
