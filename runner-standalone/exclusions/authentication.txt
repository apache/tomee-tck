# Jakarta Authentication 3.1 TCK source reactor (3.1.2) driven by the Maven
# invoker against TomEE, Java 21: 12 Web Profile modules plus the signature
# test, 106 tests. The spi module registers its test AuthConfigProvider under
# Tomcat's app-context naming (Catalina/localhost /spitests_servlet_web), so
# the server-side SAM runs and ServletProfileSPITest passes 56 of its 57
# assertions. The single remaining exclusion is a TCK challenge.
#
# Format: maven-surefire/failsafe excludesFile patterns; the runner passes
# this file to every TCK module through the invoker as
# -Dsurefire.excludesFile/-Dfailsafe.excludesFile.

# ServletProfileSPITest#CheckMsgInfoKey hard-codes a Jakarta Authorization
# (JACC) requirement: it asserts the HttpServlet MessageInfo carries the
# jakarta.security.jacc.PolicyContext key. The Web Profile does not require
# Jakarta Authorization, so the key is absent on TomEE. Open TCK challenge
# jakartaee/authentication#219.
**/ee/jakarta/tck/authentication/test/basic/ServletProfileSPITest.java#CheckMsgInfoKey
