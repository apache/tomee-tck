# Jakarta Expression Language 6.0 TCK (jakarta-expression-language-tck
# 6.0.1) against Apache Tomcat's Jasper EL, the implementation bundled in
# the TomEE distribution. The suite runs green with no exclusions: 361 tests
# pass (360 functional plus the signature test). The runner supplies the
# vendor VariableMapper implementation the TCK requires through the
# variable.mapper porting property (see runner-standalone/el/pom.xml).
#
# Format: maven-surefire excludesFile patterns, applied through
# -Dtck.exclusions.file (see runner-standalone/README.md).
