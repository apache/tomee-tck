<!--
    Licensed to the Apache Software Foundation (ASF) under one or more
    contributor license agreements. See the NOTICE file distributed with
    this work for additional information regarding copyright ownership.
    The ASF licenses this file to You under the Apache License, Version 2.0.
-->
<!--
    TomEE remote profile fragment for the Jakarta Faces TCK's modern
    Arquillian modules. Not a standalone XML document: the antrun task in the
    runner module loads it with <expandproperties/> and injects it into the
    downloaded TCK's parent pom right before </profiles>.
-->
<profile>
    <id>tomee-remote</id>

    <properties>
        <!-- The faces-signaturetest module verifies the Faces API signatures
             of the implementation the vendor ships; point it at the Mojarra
             artifact bundled in TomEE Plume (see faces.impl.version in the
             runner pom). -->
        <sigtest.api.groupId>org.glassfish</sigtest.api.groupId>
        <sigtest.api.artifactId>jakarta.faces</sigtest.api.artifactId>
        <sigtest.api.version>${faces.impl.version}</sigtest.api.version>
    </properties>

    <repositories>
        <repository>
            <id>apache.snapshots</id>
            <url>https://repository.apache.org/content/repositories/snapshots/</url>
            <releases><enabled>false</enabled></releases>
            <snapshots><enabled>true</enabled></snapshots>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>org.apache.tomee</groupId>
            <artifactId>apache-tomee</artifactId>
            <version>${tomee.version}</version>
            <type>zip</type>
            <classifier>plume</classifier>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.tomee</groupId>
            <artifactId>arquillian-tomee-remote</artifactId>
            <version>${tomee.version}</version>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.tomee</groupId>
                    <artifactId>arquillian-openejb-transaction-provider</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- The TCK's modern modules run every IT (and the signature test)
                 through the failsafe plugin, so the Arquillian remote container
                 configuration and its TomEE system properties must live in the
                 failsafe execution, not surefire; a surefire block never
                 reaches the integration-test fork and the adapter falls back to
                 its defaults (webprofile classifier, ephemeral working dir). -->
            <plugin>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <systemPropertyVariables>
                        <tomee.stopPort>-1</tomee.stopPort>
                        <tomee.httpPort>${tomee.http.port}</tomee.httpPort>
                        <tomee.ajpPort>-1</tomee.ajpPort>
                        <tomee.dir>target/tomee/server</tomee.dir>
                        <tomee.appWorkingDir>target/tomee/arquillian</tomee.appWorkingDir>
                        <tomee.cleanOnStartUp>true</tomee.cleanOnStartUp>
                        <!-- Plume ships Mojarra, the implementation the TCK
                             was developed against. -->
                        <tomee.classifier>plume</tomee.classifier>
                        <tomee.properties>openejb.environment.default=true</tomee.properties>
                        <!-- The faces40/41 WARs bundle util-4.1.2.jar, whose
                             BaseITNG @Deployment method references ShrinkWrap
                             types (org.jboss.shrinkwrap.api.Archive) that the
                             WARs do not package. OpenWebBeans loads every
                             annotated class while scanning for CDI beans and
                             aborts the deployment with a
                             ClassNotFoundException where GlassFish/Weld
                             tolerates the unloadable type. Stage the ShrinkWrap
                             API the TCK's util module already resolves (1.2.6)
                             into the server so the scan succeeds. -->
                        <tomee.additionalLibs>mvn:org.jboss.shrinkwrap:shrinkwrap-api:1.2.6
mvn:org.jboss.shrinkwrap:shrinkwrap-spi:1.2.6
mvn:org.jboss.shrinkwrap:shrinkwrap-impl-base:1.2.6</tomee.additionalLibs>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</profile>
