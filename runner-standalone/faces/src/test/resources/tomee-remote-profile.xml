<!--
    Licensed to the Apache Software Foundation (ASF) under one or more
    contributor license agreements. See the NOTICE file distributed with
    this work for additional information regarding copyright ownership.
    The ASF licenses this file to You under the Apache License, Version 2.0.
-->
<!--
    TomEE remote profile fragment for the Jakarta Faces TCK's modern
    Arquillian modules. Not a standalone XML document: the antrun task in the
    runner module loads it with <expandproperties/> and injects it into the
    downloaded TCK's parent pom right before </profiles>.
-->
<profile>
    <id>tomee-remote</id>

    <repositories>
        <repository>
            <id>apache.snapshots</id>
            <url>https://repository.apache.org/content/repositories/snapshots/</url>
            <releases><enabled>false</enabled></releases>
            <snapshots><enabled>true</enabled></snapshots>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>org.apache.tomee</groupId>
            <artifactId>apache-tomee</artifactId>
            <version>${tomee.version}</version>
            <type>zip</type>
            <classifier>plume</classifier>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.tomee</groupId>
            <artifactId>arquillian-tomee-remote</artifactId>
            <version>${tomee.version}</version>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.tomee</groupId>
                    <artifactId>arquillian-openejb-transaction-provider</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <systemPropertyVariables>
                        <tomee.stopPort>-1</tomee.stopPort>
                        <tomee.httpPort>8080</tomee.httpPort>
                        <tomee.ajpPort>-1</tomee.ajpPort>
                        <tomee.dir>target/tomee/server</tomee.dir>
                        <tomee.appWorkingDir>target/tomee/arquillian</tomee.appWorkingDir>
                        <tomee.cleanOnStartUp>true</tomee.cleanOnStartUp>
                        <!-- Plume ships Mojarra, the implementation the TCK
                             was developed against. -->
                        <tomee.classifier>plume</tomee.classifier>
                        <tomee.properties>openejb.environment.default=true</tomee.properties>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</profile>
