<?xml version="1.0" encoding="UTF-8"?>
<arquillian xmlns="http://jboss.org/schema/arquillian"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="http://jboss.org/schema/arquillian http://jboss.org/schema/arquillian/arquillian_1_0.xsd">
  <engine>
    <property name="deploymentExportPath">target/deployments</property>
  </engine>
  <!-- The adapter rewrites the ports of the overlay's server.xml to the
       configured values, so overriding tomee.http.port/tomee.https.port/
       tomee.shutdown.port on the Maven command line relocates the whole
       container. -->
  <container qualifier="tomee-remote" default="true">
    <configuration>
      <property name="groupId">org.apache.tomee</property>
      <property name="artifactId">apache-tomee</property>
      <property name="version">${tomee.version}</property>
      <property name="classifier">${tomee.classifier}</property>
      <property name="type">zip</property>
      <property name="conf">${project.build.directory}/tomee-overlay/conf</property>
      <property name="lib">${project.build.directory}/tomee-overlay/lib</property>
      <property name="host">localhost</property>
      <property name="httpPort">${tomee.http.port}</property>
      <property name="httpsPort">${tomee.https.port}</property>
      <property name="stopPort">${tomee.shutdown.port}</property>
      <property name="cleanOnStartUp">true</property>
      <property name="removeUnusedWebapps">true</property>
      <property name="simpleLog">true</property>
      <!-- The suite deploys and undeploys more than a hundred archives in
           one server lifetime. openejb.jaxrs.fail-on-constrainedto=false makes
           TomEE's CxfRsHttpListener ignore a bundled @ConstrainedTo(CLIENT)
           provider (the spec-required behavior) instead of rejecting the
           deployment; this is a server-JVM property, so it rides catalina_opts
           into the remote container rather than the surefire client JVM. -->
      <property name="catalina_opts">-Xmx2048m -XX:MaxMetaspaceSize=1g -Dopenejb.jaxrs.fail-on-constrainedto=false --add-exports=java.base/jdk.internal.vm.annotation=ALL-UNNAMED</property>
    </configuration>
  </container>
</arquillian>
