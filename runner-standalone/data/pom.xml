<?xml version="1.0" encoding="UTF-8"?>
<!--
    Licensed to the Apache Software Foundation (ASF) under one or more
    contributor license agreements. See the NOTICE file distributed with
    this work for additional information regarding copyright ownership.
    The ASF licenses this file to You under the Apache License, Version 2.0.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.apache.tomee.cts</groupId>
    <artifactId>runner-standalone</artifactId>
    <version>11.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>runner-standalone-data</artifactId>
  <name>Jakarta Data 1.0 TCK runner</name>
  <description>
    Runs the standalone Jakarta Data 1.0 TCK (web profile, persistence entity
    type) against the TomEE distribution selected by tomee.classifier.
  </description>

  <properties>
    <!-- 1.0.1 is the current service release required for certification; it
         adds the RELATIONAL jakarta.tck.database.type gate and the enum-sort
         rewrite (NaturalNumber @Enumerated(STRING) + numTypeOrdinal). -->
    <data.tck.version>1.0.1</data.tck.version>
    <!-- Reviewed known-gap exclusions; point at exclusions/none.txt for a
         full baseline run. -->
    <tck.exclusions.file>${project.basedir}/../exclusions/data.txt</tck.exclusions.file>
  </properties>

  <dependencies>
    <!-- TomEE remote Arquillian adapter and client-side runtime. -->
    <dependency>
      <groupId>org.apache.tomee</groupId>
      <artifactId>arquillian-tomee-remote</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.openwebbeans</groupId>
      <artifactId>openwebbeans-impl</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.arquillian.junit5</groupId>
      <artifactId>arquillian-junit5-container</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>jakarta.data</groupId>
      <artifactId>jakarta.data-tck</artifactId>
      <version>${data.tck.version}</version>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>jakarta.data</groupId>
      <artifactId>jakarta.data-api</artifactId>
      <version>1.0.1</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>jakarta.tck</groupId>
      <artifactId>sigtest-maven-plugin</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.shrinkwrap.resolver</groupId>
      <artifactId>shrinkwrap-resolver-impl-maven</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- The generated persistence unit runs on java:comp/DefaultDataSource,
         which src/tomee-conf-filtered/tomee.xml defines as an explicit
         embedded-Derby resource; that engine jar is staged into
         tomee-overlay/lib here (the shared parent stages only the Derby
         client). -->
    <dependency>
      <groupId>org.apache.derby</groupId>
      <artifactId>derby</artifactId>
      <version>${derby.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Inherit the shared TomEE/Derby container lifecycle. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>stage-derby-embedded</id>
            <phase>generate-test-resources</phase>
            <goals><goal>copy-dependencies</goal></goals>
            <configuration>
              <skip>${tck.standalone.container.skip}</skip>
              <includeGroupIds>org.apache.derby</includeGroupIds>
              <includeArtifactIds>derby</includeArtifactIds>
              <includeScope>test</includeScope>
              <outputDirectory>${project.build.directory}/tomee-overlay/lib</outputDirectory>
              <overWriteIfNewer>true</overWriteIfNewer>
            </configuration>
          </execution>
          <execution>
            <id>stage-tomee-api-jar</id>
            <phase>generate-test-resources</phase>
            <goals><goal>unpack</goal></goals>
            <configuration>
              <skip>${tck.standalone.skip}</skip>
              <artifactItems>
                <artifactItem>
                  <groupId>org.apache.tomee</groupId>
                  <artifactId>apache-tomee</artifactId>
                  <version>${tomee.version}</version>
                  <classifier>${tomee.classifier}</classifier>
                  <type>zip</type>
                  <outputDirectory>${project.build.directory}/tomee</outputDirectory>
                  <includes>**/lib/jakartaee-api-*.jar</includes>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>locate-tomee-api-jar</id>
            <phase>generate-test-resources</phase>
            <goals><goal>run</goal></goals>
            <configuration>
              <skip>${tck.standalone.skip}</skip>
              <exportAntProperties>true</exportAntProperties>
              <target>
                <path id="tomee-api-jar">
                  <fileset dir="${project.build.directory}/tomee">
                    <include name="**/lib/jakartaee-api-*.jar"/>
                  </fileset>
                </path>
                <pathconvert pathsep="${path.separator}"
                             property="data.sig.api.jar"
                             refid="tomee-api-jar"/>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <skip>${tck.standalone.skip}</skip>
          <dependenciesToScan>
            <dependency>jakarta.data:jakarta.data-tck</dependency>
          </dependenciesToScan>
          <groups>web &amp; persistence</groups>
          <excludesFile>${tck.exclusions.file}</excludesFile>
          <reportNameSuffix>web</reportNameSuffix>
          <systemPropertyVariables>
            <jakarta.tck.profile>core</jakarta.tck.profile>
            <jakarta.tck.skip.deployment>false</jakarta.tck.skip.deployment>
            <!-- Data 1.0.1 gates its keyword-support assertions on the
                 database type; RELATIONAL matches the Derby-backed default
                 persistence unit. The in-container EntityTests read this from
                 the container JVM (set via arquillian.xml catalina_opts); it
                 is repeated here for the client-side deployment/signature
                 executions. -->
            <jakarta.tck.database.type>RELATIONAL</jakarta.tck.database.type>
            <jimage.dir>${project.build.directory}/jimage</jimage.dir>
            <signature.sigTestClasspath>${data.sig.api.jar}</signature.sigTestClasspath>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
