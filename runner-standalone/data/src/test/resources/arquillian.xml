<?xml version="1.0" encoding="UTF-8"?>
<arquillian xmlns="http://jboss.org/schema/arquillian"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="http://jboss.org/schema/arquillian http://jboss.org/schema/arquillian/arquillian_1_0.xsd">
  <engine>
    <property name="deploymentExportPath">target/deployments</property>
  </engine>
  <container qualifier="tomee-remote" default="true">
    <configuration>
      <property name="groupId">org.apache.tomee</property>
      <property name="artifactId">apache-tomee</property>
      <property name="version">${tomee.version}</property>
      <property name="classifier">${tomee.classifier}</property>
      <property name="type">zip</property>
      <property name="conf">${project.build.directory}/tomee-overlay/conf</property>
      <property name="lib">${project.build.directory}/tomee-overlay/lib</property>
      <property name="host">localhost</property>
      <!-- The adapter reuses any server already answering on httpPort, so
           override tomee.http.port/tomee.shutdown.port to run next to
           another harness instance. -->
      <property name="httpPort">${tomee.http.port}</property>
      <property name="httpsPort">${tomee.https.port}</property>
      <property name="stopPort">${tomee.shutdown.port}</property>
      <property name="cleanOnStartUp">true</property>
      <property name="removeUnusedWebapps">true</property>
      <property name="simpleLog">true</property>
      <!-- The Data TCK relies on auto-detected JUnit extensions inside the
           container and extracts the JDK image for its signature test. -->
      <!-- jakarta.tck.database.type gates Data 1.0.1's keyword-support
           assertions; EntityTests runs in-container and reads it from the
           container JVM. RELATIONAL matches the embedded-Derby default
           persistence unit. -->
      <property name="catalina_opts">-Djunit.jupiter.extensions.autodetection.enabled=true -Djimage.dir=${project.build.directory}/jimage -Djakarta.tck.database.type=RELATIONAL --add-exports=java.base/jdk.internal.vm.annotation=ALL-UNNAMED</property>
      <!-- Declares the java:comp/Default* resource-env-refs (incl.
           DefaultDataSource used by the generated persistence.xml); without
           it @Resource(lookup="java:comp/Default...") injections stay null
           (BuiltInEnvironmentEntries in openejb-core). -->
      <property name="properties">openejb.environment.default=true</property>
    </configuration>
  </container>
</arquillian>
