<?xml version="1.0" encoding="UTF-8"?>
<!--
    Licensed to the Apache Software Foundation (ASF) under one or more
    contributor license agreements. See the NOTICE file distributed with
    this work for additional information regarding copyright ownership.
    The ASF licenses this file to You under the Apache License, Version 2.0.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.apache.tomee.cts</groupId>
    <artifactId>runner-standalone</artifactId>
    <version>11.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>runner-standalone-validation</artifactId>
  <name>Jakarta Validation 3.1 TCK runner</name>
  <description>
    Runs the standalone Jakarta Validation 3.1 TCK against the TomEE
    distribution selected by tomee.classifier, with Apache BVal as the
    provider under test.
  </description>

  <properties>
    <validation.tck.version>3.1.1</validation.tck.version>
    <cdi.tck.version>4.1.0</cdi.tck.version>
    <testng.version>7.9.0</testng.version>
    <!-- The Jakarta Validation provider shipped by TomEE. -->
    <validation.provider>org.apache.bval.jsr.ApacheValidationProvider</validation.provider>
    <!-- Reviewed known-gap exclusions; -Dtck.exclusions.file=none for a
         full baseline run. -->
    <tck.exclusions.file>${project.basedir}/../exclusions/validation.txt</tck.exclusions.file>
  </properties>

  <dependencies>
    <!-- TomEE remote Arquillian adapter and client-side runtime. -->
    <dependency>
      <groupId>org.apache.tomee</groupId>
      <artifactId>arquillian-tomee-remote</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.openwebbeans</groupId>
      <artifactId>openwebbeans-impl</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- TestNG listeners referenced from suite.xml. -->
    <dependency>
      <groupId>jakarta.enterprise</groupId>
      <artifactId>cdi-tck-core-impl</artifactId>
      <version>${cdi.tck.version}</version>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <groupId>jakarta.tck</groupId>
          <artifactId>audit</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <!-- The published TCK jar is compiled against the old ListAssert
         covariant signatures; 3.7.0 is the version the reference runner
         pins for exactly this reason. -->
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>3.7.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testng</groupId>
      <artifactId>testng</artifactId>
      <version>${testng.version}</version>
      <scope>test</scope>
    </dependency>
    <!-- Applies the reviewed exclusion list to the TestNG suite run. -->
    <dependency>
      <groupId>org.apache.tomee.cts</groupId>
      <artifactId>runner-standalone-tck-common</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.arquillian.testng</groupId>
      <artifactId>arquillian-testng-container</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.shrinkwrap.resolver</groupId>
      <artifactId>shrinkwrap-resolver-impl-maven</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Drives the mandatory Validation API signature check; the jakarta.tck
         fork understands JDK 21 class files. -->
    <dependency>
      <groupId>jakarta.tck</groupId>
      <artifactId>sigtest-maven-plugin</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Inherit the shared TomEE/Derby container lifecycle. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <!-- Stage the uber API jar the TomEE distribution ships so the
               signature test checks the jakarta.validation packages actually
               deployed, not the TCK's own copy. -->
          <execution>
            <id>stage-tomee-api-jar</id>
            <phase>generate-test-resources</phase>
            <goals><goal>unpack</goal></goals>
            <configuration>
              <skip>${tck.standalone.skip}</skip>
              <artifactItems>
                <artifactItem>
                  <groupId>org.apache.tomee</groupId>
                  <artifactId>apache-tomee</artifactId>
                  <version>${tomee.version}</version>
                  <classifier>${tomee.classifier}</classifier>
                  <type>zip</type>
                  <outputDirectory>${project.build.directory}/tomee</outputDirectory>
                  <includes>**/lib/jakartaee-api-*.jar</includes>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
          <!-- The TCK ships the reference API signatures at its jar root. -->
          <execution>
            <id>stage-validation-sig</id>
            <phase>generate-test-resources</phase>
            <goals><goal>unpack</goal></goals>
            <configuration>
              <skip>${tck.standalone.skip}</skip>
              <artifactItems>
                <artifactItem>
                  <groupId>jakarta.validation</groupId>
                  <artifactId>validation-tck-tests</artifactId>
                  <version>${validation.tck.version}</version>
                  <type>jar</type>
                  <outputDirectory>${project.build.directory}/sig</outputDirectory>
                  <includes>validation-api-jdk17.sig</includes>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <!-- The uber API jar carries every Jakarta EE spec package; the
               signature check must see only jakarta.validation, so extract that
               subtree into its own directory. -->
          <execution>
            <id>extract-validation-api-classes</id>
            <phase>generate-test-resources</phase>
            <goals><goal>run</goal></goals>
            <configuration>
              <skip>${tck.standalone.skip}</skip>
              <target>
                <path id="tomee-api-jar">
                  <fileset dir="${project.build.directory}/tomee">
                    <include name="**/lib/jakartaee-api-*.jar"/>
                  </fileset>
                </path>
                <pathconvert property="validation.sig.api.jar"
                             refid="tomee-api-jar"/>
                <unzip src="${validation.sig.api.jar}"
                       dest="${project.build.directory}/api-classes">
                  <patternset>
                    <include name="jakarta/validation/**"/>
                  </patternset>
                </unzip>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <skip>${tck.standalone.skip}</skip>
          <suiteXmlFiles>
            <suiteXmlFile>${project.basedir}/suite.xml</suiteXmlFile>
          </suiteXmlFiles>
          <dependenciesToScan>
            <dependency>jakarta.validation:validation-tck-tests</dependency>
          </dependenciesToScan>
          <!-- Suite-XML runs ignore excludesFile; the transformer listener
               applies the reviewed exclusion list instead. -->
          <properties>
            <property>
              <name>listener</name>
              <value>org.apache.tomee.tck.standalone.ExclusionsAnnotationTransformer</value>
            </property>
          </properties>
          <systemPropertyVariables>
            <validation.provider>${validation.provider}</validation.provider>
            <tck.exclusions.file>${tck.exclusions.file}</tck.exclusions.file>
          </systemPropertyVariables>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>org.apache.maven.surefire</groupId>
            <artifactId>surefire-testng</artifactId>
            <version>${maven.surefire.plugin.version}</version>
          </dependency>
        </dependencies>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <!-- These artifacts come from the EFTL distribution installed by the
           matching -install module; keep them out of the default reactor so
           a fresh clone builds without running any TCK. -->
      <id>standalone-tck</id>
      <activation>
        <property>
          <name>tck.standalone</name>
          <value>true</value>
        </property>
      </activation>
      <dependencies>
        <dependency>
          <groupId>jakarta.validation</groupId>
          <artifactId>validation-tck-tests</artifactId>
          <version>${validation.tck.version}</version>
          <scope>test</scope>
        </dependency>

      </dependencies>
      <build>
        <plugins>
          <!-- Mandatory Validation API signature test. The TCK ships the
               reference signatures as validation-api-jdk17.sig at the root of
               validation-tck-tests; the jakarta.validation packages checked
               are the ones the staged TomEE uber API jar carries. -->
          <plugin>
            <groupId>jakarta.tck</groupId>
            <artifactId>sigtest-maven-plugin</artifactId>
            <version>${sigtest.maven.plugin.version}</version>
            <executions>
              <execution>
                <id>validation-api-signature-check</id>
                <phase>test</phase>
                <goals><goal>check</goal></goals>
                <configuration>
                  <sigfile>${project.build.directory}/sig/validation-api-jdk17.sig</sigfile>
                  <packages>jakarta.validation</packages>
                  <classes>${project.build.directory}/api-classes</classes>
                  <release>17</release>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>

